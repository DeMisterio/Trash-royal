# Sounds Folder

Drop your MP3/OGG/WAV assets here. The web client automatically looks for:

- `menu.mp3` → plays on the home screen / menus
- `battle.mp3` → fades in once a battle starts

Additional files can be referenced manually from the UI configuration.
